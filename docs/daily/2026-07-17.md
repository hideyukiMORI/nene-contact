# 2026-07-17

前日 2026-07-16 の本番 Fatal の残骸から始まり、真因の根治から新機能の launch 導線まで一日で片付けた。AYANE の URL 差替（確定）→ update-contact-form CLI → composer.lock 正常化 → A 正規化フルデプロイ → 単独フォームページ → フリート横断の日報慣習文書、の順。本番 mutation は毎回 GO 前停止ゲートと可逆バックアップを通し、判断ミス・測定ミスは自分で再検証して実害ゼロで訂正した。数字は〔実測〕（自分で計測）と〔伝聞〕（他リナ/records から受領）を書き分ける。

## AYANE フォームの実 URL 差替（確定）

- board due:2026-07-16 の案件。autoreply の 3 PDF リンクと description の PP リンクを、records の確定 media URL へ差替。
- records から「media 永続化済み・3 PDF は canonical・#896 クローズ」を受領〔伝聞〕。当初は暫定扱いだったが確定扱いに格上げ。
- 手法: 本番 DB の現在値を読み、旧 URL→確定 URL を機械置換して patch を生成（手打ちの文言ズレを排除）。監査 `contact_form.updated` #8〔実測〕。
- 検証: 公開スキーマ・DB とも反映、確定 URL 4 本 200〔実測〕、テスト送信 → `autoreply.sent` #10 → Gmail 着弾〔実測〕→ ソフト削除 `submission.deleted` #11。

## update-contact-form CLI（#378 → #379）

- ライブフォームを部分更新する道具が無かった（create CLI は create 専用）。差替の前提として新設。
- `tools/update-contact-form.php` ＋ `ContactFormBodyPatch`（部分マージの SSOT・identity/未知キーは reject）。監査付き `UpdateContactFormUseCase` 経由。
- `composer check` 235 tests 緑〔実測〕。CLI をローカルで実駆動して 6 エラー経路・dry-run・実更新でフィールド/appearance 保持を確認〔実測〕。

## composer.lock 正常化（#380 → #381）— フルデプロイ Fatal の真因根治

- A フルデプロイを試みたところ本番が `enableAuthorizationHeaderFallback` で Fatal → 即ロールバック（実害ゼロ）。
- 真因: #376 は NENE2 v1.11.0 を要するのに、**contact だけ composer.lock を .gitignore していた**（Phase 0 からの path 時代の名残り・#358 移行漏れ）。兄弟 5 リポは全て lock を追跡〔実測〕。
- CI が緑だったのは lock 非追跡ゆえ CI が Packagist 最新を毎回 install していたから（`../NENE2` clone は未使用残骸・#377 にコメント）〔実測〕。
- 修正: .gitignore から外して composer.lock を v1.11.0 で追跡開始。mount を外し Packagist vendor だけで `composer check` 235 緑を実測、v1.10.0 での Fatal も同条件で再現〔実測〕。

## A 正規化フルデプロイ — 本番を main/v1.11.0 に一致

- #374 console ＋ #376 X-Auth ＋ #379 CLI を反映。vendor v1.11.0 込み。
- 転送: rsync は heteml が大量転送を切るため、vendor（59MB）を 12MB の tar.gz で scp→展開、console 等は rest.tgz で。src は rsync。
- ゲート: DB dump 退避・phinx status pending=0・backup tar t・退避→削除→展開。
- 検証: 本番 `RuntimeApplicationFactory` に `enableAuthorizationHeaderFallback` present（前回 Fatal 原因の解消）〔実測〕、公開スキーマ JSON・console 新ハッシュ・URL 差替保持・確定 4 URL 200・テスト送信 → `autoreply.sent` #16 → Gmail 着弾 → ソフト削除〔実測〕。

## 単独フォームページ（#382 → #383）— launch の実フォーム導線

- records の bespoke sanitizer が embed の script/form を剥ぐ〔伝聞〕。回避策として `GET /form/{public_form_key}` を新設し、embed.js を inline で載せた最小 HTML を返す。
- CSP の罠: 既定 `default-src 'self'` だと embed の shadow-DOM inline style と Zen Kaku がブロックされフォームが描画されない。ページ scoped の CSP で style/font だけ緩和（script は 'self'+SRI 維持）。
- ページ chrome は ENGINEERED 最低限（生成地 #efebe1 / 墨 #14120f / Zen Kaku）。
- 検証: `composer check` 235 緑〔実測〕、実ブラウザでフォーム描画（4 フィールド）・CSP 違反 0・生成地背景〔実測〕、送信テスト → Gmail 着弾 → ソフト削除。URL `https://contact.ayane.co.jp/form/ayane-contact` を records/hub に共有。
- ★教訓: フォーム font が Times に見えたのは、font 未指定の外側ラッパ `.pv-embed` を測った誤り。実フォーム `.pv-form` は正しく Helvetica sans〔実測・再測定〕。**getComputedStyle の対象要素を取り違えると別物を測る** — 検証は測る対象自体を検証する。

## 日報慣習文書（フリート横断発注）

- 18 リポの日報慣習を read-only 棚卸し〔実測〕→ 統一提案書（`_work/reports/2026-07-17-fleet-daily-report-unification-proposal.md`）→ hub APPROVE → 確定正本 `_work/daily-report-convention.md`。
- 発見: 置き場 3 系統・見出しバラバラ・正本候補 records が新旧 2 ディレクトリで二重・📊 は未浸透（records 2/16・payout 1/1）〔実測〕。正本は既に fleet-tooling に定義済みで「決める」でなく「展開する」話だった。
- この日報が contact の `docs/daily/` 初ファイル（handoff からの移行第一歩）。

## 📊 本日の数字

- マージした PR: #379・#381・#383（＋日報 #384）〔実測〕
- 本番デプロイ: A 正規化フルデプロイ 1 回（＋ロールバック 1 回）〔実測〕
- `composer check`: 235 tests / phpstan lv8 0 / cs / openapi / mcp / audit-gate / conformance 緑〔実測〕
- 本番監査イベント: `contact_form.updated` #8、`autoreply.sent` #10/#13/#16、`submission.deleted` #11/#14/#16〔実測〕
