{
  // Dependency-vulnerability gate for CI (replaces the bare `npm audit --audit-level=high`,
  // which has no way to record a reasoned exception).
  //
  // The gate stays sharp: anything high/critical that is NOT listed below fails the build.
  // `allowlist` is per-advisory (GHSA id), never per-severity — we do not lower the level.
  //
  // Adding an entry requires, in this file: (1) the advisory id, (2) why it does not apply to
  // THIS codebase, measured — not assumed, (3) an expiry date, (4) what removes the need for it.
  // An expired entry is a task, not a renewal: re-check it, do not extend it by reflex.
  "$schema": "https://github.com/IBM/audit-ci/raw/main/docs/schema.json",
  "moderate": false,
  "high": true,
  "critical": true,
  "allowlist": [
    // GHSA-mh99-v99m-4gvg — brace-expansion "DoS via unbounded expansion length" (high).
    // Vulnerable range <=5.0.7, i.e. it covers the whole v1 and v2 lines, which have NO fixed
    // release (v1 stops at 1.1.16, v2 at 2.1.3). Only the v5 line has a fix, and we take it:
    // the override is `brace-expansion@5: ^5.0.8`, scoped per major.
    //
    // 🔴 Do NOT "fix" this by collapsing the override to a flat `brace-expansion: ^5.0.8`.
    // That forces v5 onto `minimatch@3`, which expects the v1 CJS export and dies with
    // `TypeError: expand is not a function`. It happened here (#530 → #538) and lint stayed
    // green, because minimatch@3 only reaches brace-expansion for patterns containing `{...}`.
    // `npm run check:overrides` now probes exactly that.
    //
    // Why the residue does not apply here (measured 2026-07-29): brace-expansion v1/v2 reach
    // us only through **dev tooling** — eslint-plugin-import / eslint-plugin-jsx-a11y
    // (minimatch@3) and @redocly/openapi-core (minimatch@5). `npm ls brace-expansion
    // --omit=dev` is empty and the built console bundle contains zero occurrences, so no
    // version of it is shipped to a browser. The DoS needs attacker-controlled glob patterns;
    // ours are static config in the repo, expanded on our own CI machines.
    //
    // Expiry: 2026-08-31. Removed when the eslint plugins move off minimatch@3 (or publish a
    // patched v1/v2 line). Re-check at expiry — do not extend by reflex.
    "GHSA-mh99-v99m-4gvg",

    // GHSA-qwww-vcr4-c8h2 — react-router "RSC Mode CSRF Bypass Allows Action Execution Before
    // 400 Response" (high). Vulnerable range 7.12.0–8.2.0; there is NO fix in the 7.x line
    // (react-router-dom stops at 7.18.1; the fix is react-router v8 >= 8.2.1, a different
    // package and a breaking upgrade).
    //
    // Why it does not apply here (measured 2026-07-29): this admin console is a **static SPA
    // built by Vite** and served from `public_html/console/`. It uses `createBrowserRouter`
    // with element-only routes — no RSC mode, no server components, no route `action`/`loader`
    // running on a server, and no `@react-router/dev` server runtime. The advisory's attack
    // path (a server executing a route action before returning 400) has no counterpart in a
    // client-only bundle. Verified by grepping the router for `action:` / `loader:` (none) and
    // by the absence of any react-router server entry in the build.
    //
    // Expiry: 2026-08-31. Removed by the react-router v8 migration wave (bundled with the
    // NENE2 RR8 re-evaluation). If that wave slips past the expiry, the entry must be
    // re-argued in a PR — not silently extended.
    "GHSA-qwww-vcr4-c8h2",
  ],
}
